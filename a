Surplus_FechaMaxSelector :=
VAR FechaMaxSelector =
    MAX ( Dates[Date] )          -- fecha máxima que queda después del slicer
RETURN
CALCULATE (
    SUM ( escenarios_stress[surplus] ),
    Dates[Date] = FechaMaxSelector
)

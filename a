Comentario limpio =
VAR txt = 'comment_js_vw'[comment_highlights]
VAR salto = UNICHAR(10)

// 1. Eliminar la palabra "Highlights" y el salto de línea inmediatamente posterior (si lo hay)
VAR sinHighlights =
    SUBSTITUTE(txt, "Highlights" & salto, "")
VAR sinHighlights2 =
    SUBSTITUTE(sinHighlights, "Highlights", "")

// 2. Eliminar saltos de línea vacíos (saltos múltiples → solo uno)
VAR sinSaltosVacios =
    SUBSTITUTE(
        SUBSTITUTE(
            SUBSTITUTE(sinHighlights2, salto & salto & salto, salto),
            salto & salto, salto
        ),
        salto & salto, salto
    )

RETURN sinSaltosVacios

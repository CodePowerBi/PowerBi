ID_Repetido :=
VAR _id = fusion_vw[id]
VAR _count =
    CALCULATE (
        COUNTROWS ( fusion_vw ),
        FILTER ( fusion_vw, fusion_vw[id] = _id )
    )
RETURN
IF ( _count > 1, "Repetido", "No repetido" )

official_nueva_fmt :=
VAR RawVal = [official_nueva]  -- tu medida de cálculo
VAR PercentVal = RawVal * 100
RETURN
IF(
    PercentVal = INT(PercentVal),
    FORMAT(PercentVal, "0\%"),
    FORMAT(PercentVal, "0.00\%")
)

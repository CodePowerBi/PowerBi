high_eve_SALTOS =
VAR salto = UNICHAR(10) & UNICHAR(10)  -- doble salto entre bloques
VAR high  = COALESCE([high_SALTOS], "")
VAR eve   = COALESCE([eve_SALTOS], "")

VAR unido =
    high &
    IF(high <> "" && eve <> "", salto, "") &
    eve

-- Quita espacios y evita devolver cadena vacía
RETURN IF(LEN(TRIM(unido)) = 0, BLANK(), TRIM(unido))

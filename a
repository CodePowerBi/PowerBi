Cantidad_Anterior = 
VAR FechaActual = km1[date]
VAR FechaAnterior = 
    CALCULATE(
        MAX(km1[date]),
        FILTER(
            km1,
            km1[date] < FechaActual
        )
    )
RETURN
    CALCULATE(
        SUM(km1[cantidad]),
        km1[date] = FechaAnterior
    )

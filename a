BgColor_Assets_Inflows :=
VAR m = SELECTEDVALUE ( 'dicc_metrica_eng'[metrica], "" )
RETURN
IF (
    CONTAINSSTRING ( m, "High-Quality liquidity Assets" )
        || CONTAINSSTRING ( m, "Cash inflows" ),
    "#E6E6E6",   -- gris claro
    BLANK()      -- sin color (deja el fondo por defecto)
)

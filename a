VALUE_nueva :=
VAR MaxYM = [MesMax_Slicer]
VAR TenorSel = SELECTEDVALUE( slide_values_vw[tenor] )
RETURN
CALCULATE(
    COALESCE( SUM( slide_values_vw[valor] ), 0 ),
    KEEPFILTERS( 'Dates'[MesAnioOrden] = MaxYM ),
    KEEPFILTERS( slide_values_vw[tenor] = TenorSel )
)

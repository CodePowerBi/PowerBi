CleanHighlights =
VAR txt = 'comment_js_vw'[comment_highlights]
VAR textoSinHighlights =
    IF(
        LEFT(txt, 9) = "Highlights",
        MID(txt, 10, LEN(txt) - 9),
        txt
    )
RETURN TRIM(textoSinHighlights)

Título_Position_Menos_3Meses =
VAR MesSeleccionadoTexto =
    IF(
        HASONEVALUE(Dates[Trim]),
        VALUES(Dates[Trim]),
        BLANK()
    )

VAR FechaCorrespondiente =
    CALCULATE(
        MIN(Dates[Date]),
        FILTER(Dates, Dates[Trim] = MesSeleccionadoTexto)
    )

VAR FechaMenos3Meses =
    EDATE(FechaCorrespondiente, -3)

VAR TrimMenos3Meses =
    CALCULATE(
        VALUES(Dates[Trim]),
        FILTER(Dates, Dates[Date] = FechaMenos3Meses)
    )

RETURN
IF(
    NOT ISBLANK(MesSeleccionadoTexto),
    "Position " & TrimMenos3Meses,
    "Please select a month first"
)

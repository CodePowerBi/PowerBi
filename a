Nueva_Medida :=
VAR metric = SELECTEDVALUE ( mrf_resumen_evolutivo[metrica] )
RETURN
    SWITCH (
        TRUE(),
        metric IN { "almm", "lcr", "nsfr", "data_quality", "traceability" }, 0.5,
        BLANK()
    )

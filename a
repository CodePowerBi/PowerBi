Cantidad_Mes_Anterior = 
VAR FechaActual = 'TuTabla'[Fecha]
VAR FechaMesAnterior = EDATE(FechaActual, -3)
RETURN
CALCULATE(
    MAX('TuTabla'[Cantidad]),
    FILTER(
        'TuTabla',
        'TuTabla'[Fecha] = FechaMesAnterior
    )
)

VAR FechaMaxDatos =
    CALCULATE(
        MAX ( 'tabla_nueva_Cet'[fecha] ),
        FILTER(
            ALL ( 'tabla_nueva_Cet' ),
            VAR Num = VALUE(SUBSTITUTE('tabla_nueva_Cet'[Valor], "%", ""))
            RETURN NOT ISERROR(Num) && Num <> 0
        )
    )

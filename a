Price Slicer :=
VAR MaxYM = [MesMax_Slicer]

VAR FV_Mes =
    CALCULATE(
        [FV],
        KEEPFILTERS ( 'Dates'[MesAnioOrden] = MaxYM )
    )

VAR CA_Mes =
    CALCULATE(
        [Carrying Amount],
        KEEPFILTERS ( 'Dates'[MesAnioOrden] = MaxYM )
    )
RETURN
DIVIDE( FV_Mes, CA_Mes, 0 )   -- último argumento: valor si hay error (evita NaN)

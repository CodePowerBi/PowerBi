EsUltimaCalculationDate =
VAR FechaActual = 'Tabla'[fecha]
VAR MaxCalcDatePorFecha =
    CALCULATE(
        MAX ( 'Tabla'[calculation_date] ),
        FILTER(
            'Tabla',
            'Tabla'[fecha] = FechaActual
        )
    )
RETURN
    'Tabla'[calculation_date] = MaxCalcDatePorFecha

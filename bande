VAR EsTotal = MAX(BanderasConTotal[Country_Limpio]) = "Total"
RETURN
IF(
    EsTotal,
    CALCULATE(
        [Exposure],  -- o la medida base
        REMOVEFILTERS(BanderasConTotal[Country_Limpio])
    ),
    [Exposure]
)
